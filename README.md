# Chess
Typescript FE with a cpp home made chess engine BE


things to add:
- fix UI e.g. clocks
- add checkmate popup 
- fix reset game button so that the engine can play multiple games
- UI sizing doesn't work. need to get all the buttons on the screen together
- actually learm and understand engine code e.g. bitmaps, why we didn't use pointers, compiling and Cmake files
- prevent rule breaks e.g. opp castled despite my already taking the rook (spawned a new rook)
- adjust engine difficulty sliders